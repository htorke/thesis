function [aperture] = gauss(X,Y,radius,center_x=0,center_y=0)

%Optimum Strehl Ratio = 0.815

%I(w0)/I(0) = 1/e^2
w0 = 0.89*radius;
gauss = exp(-0.5*((x-u)/sigma).^2);%/(sigma*sqrt(2*pi));


normpdf(x);

aperture
plot(
end
